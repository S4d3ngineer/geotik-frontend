import { MatchPasswordDirective } from './match-fields.directive';

describe('MatchPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new MatchPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
